# Bot Rider — Workspace MCP (read-only)

WM-1–3 **unchanged** (read-only emit in `architecture-mvp.md` + Swarm MCP-read consume).

WM-4 never-invoke-write is superseded by architecture-mcp-actions.md staging + MCP-gate Approve; reads unchanged.
